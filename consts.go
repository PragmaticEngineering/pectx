package pectx

const (
	// RequestIDKey is the key used to store the request ID in the context.
	RequestIDKey = "requestID"
	// RequestMethodKey is the key used to store the request method in the context.
	RequestMethodKey = "requestMethod"
	// RequestPathKey is the key used to store the request path in the context.
	RequestPathKey = "requestPath"
)
